using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;


public class VehicleController : MonoBehaviour
{
    // This class will be added to the root object of any vehicle added to a world scene.

    // It will need to spawn a GridController to provide the grid of blcks that the root object has, then work out and declare the EnclosedSpaces and ExposedSpaces.
    // These should probably be a seperate class tht records the pressure, liquid content, gas content etc
    // It will also need to manage the camera system for third-person mode if a player is controlling the vehicle.
    
    public void Awake()
    {
        
    }

    public void Start()
    {
        
    }

    public void FixedUpdate()
    {
        
    }

    public void Update()
    {
        
    }


}
